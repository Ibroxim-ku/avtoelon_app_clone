import 'package:flutter/material.dart';

class AddDetail extends StatelessWidget {
  const AddDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("E'lon berish uchun"),
      ),
    );
  }
}
